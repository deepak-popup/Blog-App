<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<p><%= link_to "Edit", [:edit, @user, @article] %></p>
<p><%= link_to "Delete", [@user, @article], method: :delete, data: { confirm: "Are you sure?" } %></p>

<h3>Comments</h3>
<%= render @article.comments %>

<h4>Add a comment:</h4>
<%= render "comments/form" %>